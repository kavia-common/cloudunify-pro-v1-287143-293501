# BackendAPI environment variables (example)

# Example PostgreSQL URL (prefer asyncpg driver for async SQLAlchemy)
# Format: postgresql+asyncpg://USER:PASS@HOST:PORT/DBNAME
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cloudunify_db

# Enable SQL echo logs (0/1)
SQL_ECHO=0

# Prefer Alembic migrations in non-dev environments (default 1)
# Set to 0 to fallback to SQLAlchemy create_all (dev only).
USE_ALEMBIC=1

# Notes:
# - For local tests/dev without Postgres, set:
#     DATABASE_URL=sqlite+aiosqlite:///./app.db
#   The app will skip Alembic and auto-create tables on SQLite for convenience.
