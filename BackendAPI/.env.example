# BackendAPI - .env.example
# Copy to .env and adjust values for your environment. Do NOT commit real secrets.

# --- Server ---
HOST=0.0.0.0
REACT_APP_PORT=3001
REACT_APP_LOG_LEVEL=info
RELOAD=1

# --- CORS ---
# Comma-separated list of allowed origins, or * for any (dev only)
CORS_ORIGINS=*

# --- Database ---
# Dev default uses SQLite (no external DB required). For Postgres, set a proper URL.
DATABASE_URL=sqlite+aiosqlite:///./app.db
USE_ALEMBIC=0
SQL_ECHO=0

# --- Auth / JWT ---
SECRET_KEY=CHANGE_ME_DEVELOPMENT_ONLY
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- Dev Seeding ---
# Enable dev user seeding. Alias: DEV_SEED_ENABLED=1
DEV_SEED_USERS=1

# Default seeded users (for convenience)
DEV_ADMIN_EMAIL=admin@cloudunify.pro
DEV_ADMIN_PASSWORD=Admin123!
DEV_USER_EMAIL=user@cloudunify.pro
DEV_USER_PASSWORD=User123!

# Optional additional custom seed user (useful for demos)
# Example demo (do not use in production):
# DEV_SEED_EMAIL=kishore@kavia.ai
# DEV_SEED_PASSWORD=kishore15404
# DEV_SEED_ROLE=user
# DEV_SEED_ACTIVE=1

# --- Diagnostics ---
# Log sanitized authentication failures in dev
AUTH_LOG_FAILURES=1

# --- Environment flavor (enables seeding by default when 'development') ---
# ENV=development
